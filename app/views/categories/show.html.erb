<!--<%= render "navbar" %>-->

<div class="container mt-5">
<div class="row">
  <div class="mx-auto">
    <p style="color: green"><%= notice %></p>
  </div>
</div>

<% render @category %>

<div>
  <% link_to "Edit this category", edit_category_path(@category) %> <!-- | -->
  <% link_to "Back to categories", categories_path %>

  <% button_to "Destroy this category", @category, method: :delete %>
</div>

  <div class="row">    
    <div class="card col-md-8 mx-auto">
      <div class="card-body">
        <form>          
          <div class="d-flex justify-content-between">
            <div><legend>Detail</legend></div>
          <div><%= link_to "Back to categories", categories_path, class: "btn-sm btn-secondary" %></div>
          </div>
          <div class="mb-3">
            <label class="form-label">Name</label>
            <input type="text" class="form-control" value="<%= @category.name%>" readonly>            
          </div>
          <div class="d-flex justify-content-between">
            <div class="">
              <%= link_to "Edit this category", edit_category_path(@category), class: "btn btn-warning" %>
            </div>
            <div class="invisible">
              <%= button_to "Destroy this category", @category, method: :delete, class: "btn btn-danger" %>
            </div>
          </div>
          <div class="mt-2">
            <%= button_to "Destroy this category", @category, method: :delete, class: "btn btn-danger" %>
          </div> 
        </form>
      </div>
    </div>
  </div>
</div>